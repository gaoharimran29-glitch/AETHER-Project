# =============================================================================
# AETHER — Docker Compose
# National Space Hackathon 2026, IIT Delhi
#
# Usage:
#   docker compose up --build          # build and start
#   docker compose up --build -d       # detached (background)
#   docker compose down                # stop and remove containers
#   docker compose logs -f             # follow logs
#
# After starting, the system is available at:
#   API:       http://localhost:8000/api/status
#   Dashboard: http://localhost:8000/
# =============================================================================

services:
  aether:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aether
    ports:
      - "8000:8000"       # PS §8 — grading scripts connect here
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONDONTWRITEBYTECODE=1
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8000/api/status"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 40s
    restart: unless-stopped
    # Resource limits — keep image lean and predictable
    deploy:
      resources:
        limits:
          memory: 1g
          cpus: '2.0'
        reservations:
          memory: 512m
          cpus: '0.5'